# Runtime dependencies, pinned for reproducible builds.
# Upgrade deliberately: bump the pin, run the tests, rebuild.
customtkinter==6.0.0
darkdetect==0.8.0
packaging==26.3
yt-dlp==2026.8.19
# Needed by yt-dlp to embed cover art into opus and flac files.
mutagen==1.48.1
